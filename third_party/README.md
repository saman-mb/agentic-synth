# Third-Party Dependencies

This directory is reserved for vendored dependencies and git submodules.

Expected submodules:

- `JUCE/` for plugin, standalone app, and audio framework integration.
- `llama.cpp/` for local model inference.

The repository license covers original Agentic Synth source code only. Review and comply with each dependency's license before shipping commercial builds.
