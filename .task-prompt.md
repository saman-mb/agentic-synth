You are working in a clone of the agentic-synth repository at /tmp/agentic-synth-issue-02.
Your task: implement GitHub Issue #2

ISSUE TITLE: CMake build system with JUCE 7 submodule

ISSUE BODY:
Add JUCE 7 as a submodule under third_party/JUCE. Wire CMake top-level project that:
- Configures macOS (arm64+x86_64), Linux, Windows toolchains
- Defines a JUCE standalone app target and a placeholder VST3/AU target
- Honors CMAKE_BUILD_TYPE Debug/Release with sane defaults
- Sets C++20

Acceptance: 'cmake -B build && cmake --build build' produces a runnable empty standalone app on the dev machine.

INSTRUCTIONS:
1. Create a new branch: git checkout -b fix/issue-2
2. Implement the issue requirements
3. git add, git commit, git push origin fix/issue-2
4. Create a PR: gh pr create --base master --head fix/issue-2 --title "CMake build system with JUCE 7 submodule" --body "Closes #2"
5. Work directory: /tmp/agentic-synth-issue-02
